#!/usr/bin/env bash
# AppRun for the StemTube graphical installer AppImage.
HERE="$(dirname "$(readlink -f "${0}")")"
export APPDIR="$HERE"
export PATH="$HERE/usr/bin:$PATH"
export LD_LIBRARY_PATH="$HERE/usr/lib:${LD_LIBRARY_PATH:-}"
exec bash "$HERE/install-gui.sh" "$@"
